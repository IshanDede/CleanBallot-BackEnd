const {
  addCandidate,
  getCandidates,
  getCandidate,
  overwriteCandidate,
  updateCandidate,
  deleteCandidate,
} = require("./candidate");

const {
  addVoter,
  getVoters,
  getVoter,
  overwriteVoter,
  updateVoter,
  deleteVoter,
} = require("./voters");

module.exports = {
  addCandidate,
  getCandidates,
  getCandidate,
  overwriteCandidate,
  updateCandidate,
  deleteCandidate,
  addVoter,
  getVoters,
  getVoter,
  overwriteVoter,
  updateVoter,
  deleteVoter,
};
